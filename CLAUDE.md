# Position Generator

Single-page app vanilla HTML/CSS/JS. Pas de build, pas de framework.

## Structure

- `index.html` — Toute l'app (HTML + CSS + JS inline, illustrations SVG)

## Dev

Ouvrir `index.html` dans un navigateur. Aucun serveur requis.
